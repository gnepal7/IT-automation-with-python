git add -p

git commit -m 'Rename min_absolute to min_gb, use parameter names'

git push

git pull

git log --graph --oneline --all # displays all branches and commit on them.

git log -p origin/master # three-way-merge

git add all_checks.py 
git commit

git push

git log --graph --oneline
